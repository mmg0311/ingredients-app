import React from 'react';
import styled from 'styled-components';

import { Sidebar } from './';

const Tabs = () => {
    return (
        <Style>
            <Sidebar />
        </Style>
    );
}

export default Tabs;

const Style = styled.div`

`